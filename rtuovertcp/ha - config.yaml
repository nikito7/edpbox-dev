# (...)

modbus:

  - name: edpbox1
    type: rtuovertcp
    host: 10.1.0.40
    port: 8888

    sensors:
        
      - name: 'RTU Voltage L1'
        slave: 1
        address: 108
        input_type: input
        count: 1
        data_type: uint
        scale: 0.1
        precision: 1
        unit_of_measurement: 'V'

      - name: 'RTU Current L1'
        slave: 1
        address: 109
        input_type: input
        count: 1
        data_type: uint
        scale: 0.1
        precision: 1
        unit_of_measurement: 'A'

# EOF
